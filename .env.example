# Copy to `.env` and fill in your MongoDB connection string
# Example MongoDB Atlas connection string (replace placeholders):
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/guided_growth?retryWrites=true&w=majority

# Optional: PORT the server should listen on
PORT=5001
